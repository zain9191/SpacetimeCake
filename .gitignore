.DS_Store
# Installers and disk images have no business in this repo (a 1 GB DMG
# was accidentally committed once already — see d799995).
*.dmg
*.iso
*.pkg
*.mp4
*.mov
*.webm
*.avi
*.mkv
# Keep the test fixture so CI can run E2E tests deterministically
!tests/fixtures/sample.mp4
node_modules/
test-results/
playwright-report/
playwright/.cache/
blob-report/
.env
.env.local
